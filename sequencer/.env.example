# AgentL2 Sequencer
# Copy to .env and fill in. For local devnet, use the first Hardhat account key.

# L2 RPC URL (e.g. http://127.0.0.1:8545 for local devnet, or OP Stack chain RPC)
RPC_URL=http://127.0.0.1:8545
# Or explicitly: L2_RPC_URL=, L2_BRIDGE_ADDRESS=

# L2Bridge contract address (from deploy:local or deploy-l2)
BRIDGE_ADDRESS=

# L1 bridge (optional; when set, sequencer relays L1 deposits to L2 and proves L2 withdrawals on L1)
# L1_RPC_URL=https://rpc.sepolia.org
# L1_BRIDGE_ADDRESS=0x...

# Sequencer private key (must be the contract sequencer; for local use Hardhat account #0)
# Get with: npx hardhat console --network localhost -> (await ethers.getSigners())[0].privateKey
SEQUENCER_PRIVATE_KEY=

# HTTP port for the sequencer API
PORT=3040

# Poll intervals (ms)
# POLL_INTERVAL_MS=60000
# L1_POLL_INTERVAL_MS=30000
