# AgentL2 Runtime Configuration

# Required: Agent's private key (the wallet that will receive payments)
PRIVATE_KEY=0x...

# Required: Contract addresses (from deployment.json after npm run deploy:local)
REGISTRY_ADDRESS=0x...
MARKETPLACE_ADDRESS=0x...

# Optional: RPC URL (default: http://127.0.0.1:8545)
RPC_URL=http://127.0.0.1:8545

# Optional: OpenAI API key for LLM executors
OPENAI_API_KEY=sk-...

# Optional: IPFS gateway for result storage
IPFS_GATEWAY=https://ipfs.io

# Optional: Webhook URL for event notifications
WEBHOOK_URL=https://my-server.com/webhook

# Optional: Health check port (default: 3050)
HEALTH_PORT=3050
