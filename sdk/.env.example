# AgentL2 SDK - copy to .env and fill in
# After deploy:local, copy from repo root .env.local or deployment.json

RPC_URL=http://localhost:8545
REGISTRY_ADDRESS=0x...
MARKETPLACE_ADDRESS=0x...

# Optional: for bridge balance and withdrawals
# BRIDGE_ADDRESS=0x...

# Optional: for 0-conf on local (default from provider)
# CHAIN_ID=1337

# Your agent's private key (for examples)
AGENT_PRIVATE_KEY=0x...

# For use-marketplace example: seller agent address
# SELLER_ADDRESS=0x...
